[project]
name = "cfa-score-demo"
version = "0.1.0"
description = "Output-side combination fact restoration risk detector for raw LLM answers"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
cfa-score = "cfa_score.cli:main"

[tool.setuptools.packages.find]
include = ["cfa_score*"]
